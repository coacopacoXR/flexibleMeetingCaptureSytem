# flexibleMeetingCaptureSytem

## Preview the prototype

```bash
npm run dev
```

Then open <http://localhost:8000> in your browser.

The prototype relies on the browser SpeechRecognition API for live transcription, so use a compatible browser (Chrome recommended) and allow microphone access when prompted.
